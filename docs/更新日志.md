# 飞剧场怀旧麻将 — 更新日志

本文件记录 **本 fork（飞剧场）相对上游的发版说明**。  
上游官方变更见 App 内帮助 changelog，或 [seleuco/MAME4droid-Current Releases](https://github.com/seleuco/MAME4droid-Current/releases)。

用户可感知变化见 [宣传要点.md](宣传要点.md)「近期变化」；**给玩家看的白话说明**见 [用户更新说明.md](用户更新说明.md)；开发约定见 [知识库.md](知识库.md)。

**当前版本：** `1.38.1-mj8`（versionCode 155，MAME 核心 0.289，按键包 `20260819.1`）

发版时请：bump `app/build.gradle` → 在本文件顶部追加一节 → 同步宣传要点「近期变化」与 [用户更新说明.md](用户更新说明.md) →（建议）打 Git tag。

---

## 1.38.1-mj8（2026-08-19）

- 修复 `mame.lst` UTF-8 BOM 导致龙虎榜（`lhb`）假缺 ROM、定制底图不显示
- 同步按键包 `20260819.1`：`rbmk`（实战麻将王）、`lhzb4` Lua 等；打包前 `--check` 流程
- 选台底图新增/更新：雀士 Brandnew Stars、文化、雀斗记、校园狩猎、麻将学院、满贯至尊 100CN、麻将姐妹、黄金牌、电子基盘加强、超级大满贯 II、天开眼、等

---

## 1.38.1-mj7（2026-08-18）

- 假缺 ROM：zip 被系统当成文件夹时也会识别（如 `lhb.zip`）
- 选台已选 ROM 文件夹后，第一次进经典界面不再重复要选
- 菜单可勾选紧凑列表；点机种短遮罩再进游戏；菜单加帮助；最近玩过右侧小点
- 选台底图：`lhb` / `lhb2` / `lhb3` / `dbc` / `xymg`

---

## 1.38.1-mj6（2026-08-18）

- 选台首次引导：未选 ROM 文件夹时先选目录，再可选导入 snap
- 缺 ROM：扫描文件夹 zip/7z，列表可辨认；点了只提示、不进 MAME 空列表
- 最近玩过最多 3 条置顶；退出游戏后停在刚才那一行
- 顶栏 ROM / Snap / 设置 / 经典界面收进右上角菜单；竖屏遮罩改为左实右透

---

## 1.38.1-mj5（2026-08-18）

- 选台列表：克隆挂在主档下并浅灰区分；snap 识别 `rom.png` 与 `rom/0000.png`；两版缺省底图 `_default.webp`；左侧约 2/3 遮罩方便读名

---

## 1.38.1-mj4（2026-08-17）

- 自制选台首页：按键包机种列表、搜索、点选直开；行背景优先定制图 → snap → 渐变（试配 `mjelctrn.webp`）
- 固定 release 签名，支持覆盖安装（保留 ROM/snap 路径）
- 含 mj3：龙虎争霸 4 自动切 Mahjong 控制

---

## 1.38.1-mj3（2026-08-13）

- 按键包 `20260813.2`：`lhzb4` / `lhzb4dhb` 启动时自动把 DIP「Controls」切到 Mahjong（默认 Joystick 会导致麻将键全无效）

---

## 1.38.1-mj2（2026-08-13）

- 同步按键包 `20260813.1`（仓库 A：`output_proxy` 适配 MAME 0.289+ 输出 API、更新各机种 Lua、artwork 按白名单从 `mame_current` 刷新）
- 手机端仍保留横竖屏自动切 artwork View

---

## 1.38.1-mj1（2026-08-10）

- 合并上游 **MAME4droid 1.38.1**（核心 **0.289**、Android 17 / `targetSdk 37`、NetPlay 局域网权限等）
- 保留飞剧场双版本（full / basic）、按键包、麻将 UX、中文列表与路径修复
- CI：`MAME4DROID_UPSTREAM_VERSION=1.38.1`；因 Actions 暂无 `platforms;android-37`，**compileSdk 暂为 36**，targetSdk 仍为 37

---

## 1.37.6-mj16（2026-08-10）

- 工作目录默认并自动迁到可浏览的 `Android/data/<包名>/files/`（避免锁在 `/data/user/0/...`）
- 按键包安装后写入 `ui.ini`：`system_names mame.lst`，使中文游戏名真正生效
- 按键包 `20260810.3`

---

## 按键包中文名（20260810.1 / 20260810.2）

- 新增 `mahjong_pack/mame.lst`（及 `arcade.lst` 兜底）：有按键包的机种及其已知克隆约 70 条中文显示名
- 经 `AssetPackInstaller` 自动安装到 files 根目录

---

## 1.37.6-mj15

- 进阶版：更多页加 Tab 开关游戏内 MAME 菜单；工具箱增加退出
- 横屏 OSC 去掉 Start / Coin / Option / Exit（改走工具箱 / 麻将键）

---

## 1.37.6-mj14

- 进阶版：竖屏键盘再下移；横屏打牌页 N 键加宽，键间距更紧

---

## 1.37.6-mj13

- 进阶版：工具菜单改右上角；横屏打牌/更多并入左右侧栏且底对齐；竖屏键盘略下移

---

## 1.37.6-mj12

- 进阶版：可收放工具菜单（手柄 / 麻将键 / 菜单 / ROM / snap）
- 麻将键盘横屏底栏更矮更透并对齐画面；竖屏统一网格并上移

---

## 1.37.6-mj11

- 进阶版麻将键盘：横屏常驻键贴游戏画面两侧；底栏「打牌」「更多」与字母同高

---

## 1.37.6-mj10

- 进阶版麻将键盘贴近按键包布局：横屏左右常驻动作键、竖屏底栏两行常驻
- 「打牌 / 更多」只切换底部字母数字区

---

## 1.37.6-mj9

- 进阶版：侧边「打牌 / 更多」切换；打牌页 M/N 恢复字母；海底/大小挪到更多页

---

## 1.37.6-mj8

- 进阶版：通用麻将键盘「打牌 / 更多」两页
- 基础版启用独立图标背景

---

## 1.37.6-mj7

- 主界面紧凑 📁(ROM) / 🖼(snap)；基础顶中、进阶工具菜单内
- snap 可复制路径或导入到应用内 `snap/`

---

## 1.37.6-mj6

- 主界面「选择游戏文件夹」一键设 ROM 路径

---

## 1.37.6-mj5

- 官网用系统浏览器打开；星球链接提示可发到微信「文件传输助手」

---

## 1.37.6-mj4

- 基础版收紧为贴近官方（按键包等必要能力）
- 帮助增加「飞剧场」导流页

---

## 更早（摘要）

| 阶段 | 内容 |
|------|------|
| mj3 及前后 | 双 flavor、按键包自动安装、方向桥接、横屏 OSC 定制、开源合规与宣传文档 |
| 再早 | 品牌与包名 `com.feijuchang.mahjong(.basic)`、CI 打 APK |

细节以对应 Git commit / tag 为准。
