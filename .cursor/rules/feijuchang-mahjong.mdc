---
description: 飞剧场怀旧麻将 fork 的常驻约定（双版本、按键包、手柄、CI）
alwaysApply: true
---

# 飞剧场怀旧麻将

详见 `docs/知识库.md`、`docs/更新日志.md`、`docs/开源与合规.md`、`docs/宣传要点.md`。

## 身份（两套 APK，同一 Git 分支 + flavors）

- 完整版 `full`：飞剧场怀旧麻将 · `com.feijuchang.mahjong` · `FEIJUCHANG_FULL_UX=true` · 麻将体验锦上添花
- 基础版 `basic`：基础飞剧场 · `com.feijuchang.mahjong.basic` · **尽量贴近官方**；只做必要减负（按键包、主界面 📁/🖼 等）
- Java 包可保持 `com.seleuco.mame4droid`；勿冒充官方；GPL + `NOTICE`
- 不内置 ROM；按键包在 `assets/mahjong_pack/`（已开源，见该目录 LICENSE.txt）

## 宣传文档

- 用户可感知的功能**新增或变动**后，必须更新 `docs/宣传要点.md`（条目 +「近期变化」）
- 发版同时追加 `docs/更新日志.md` 顶部一节，并同步改写 `docs/用户更新说明.md`（白话、无技术黑话）

## 勿混改

- 本仓：`D:\Dev\MAME4droid-Mahjong`
- 按键包开发源：`D:\Dev\MAMEmjKey\MAME_Mahjong_Mods`（对外以本仓 mahjong_pack 为准）

## 体验门控

- 两版都跑 `AssetPackInstaller`
- **仅 full**：种子默认、方向桥接、强制竖屏、右上角工具菜单、麻将键盘、`oscForceHidden`（`MahjongExperienceHelper` 早退）
- 横屏 OSC 只在 `app/src/full/res/raw*`，且**无** Start/Coin/Option/Exit；`main` 保持上游布局，避免污染 basic
- 按钮 ID `8=Coin` `9=Start`；Exit / MAME 菜单改走工具箱 🚪 与麻将键更多页 Tab

## 导流

- 帮助「飞剧场」：官网 `https://roxyweal.work/majiang/` 系统浏览器；知识星球名「飞剧场的藏宝阁」；优惠券 `https://t.zsxq.com/35B0X`（复制链接 + 尝试微信 / 文件传输助手）

## CI / 分发

- 手动 Run **Build APK**，选 flavor；**只打 release**
- 凡放 APK 下载链接处，同时附 GitHub：https://github.com/zffffff/MAME4droid-Mahjong
